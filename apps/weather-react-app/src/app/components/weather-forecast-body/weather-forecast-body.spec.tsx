import WeatherForecastBody from './weather-forecast-body';

describe('WeatherForecastBody', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<WeatherForecastBody  cityForecast={} data$={}/>);
    // expect(baseElement).toBeTruthy();
  });
});
