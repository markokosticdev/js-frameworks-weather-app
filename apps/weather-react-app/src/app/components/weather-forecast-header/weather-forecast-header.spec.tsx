import WeatherForecastHeader from './weather-forecast-header';

describe('WeatherForecastHeader', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<WeatherForecastHeader  onSubmit={}/>);
    // expect(baseElement).toBeTruthy();
  });
});
