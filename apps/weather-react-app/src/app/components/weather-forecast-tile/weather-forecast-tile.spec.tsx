import WeatherForecastTile from './weather-forecast-tile';

describe('WeatherForecastTile', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<WeatherForecastTile />);
    // expect(baseElement).toBeTruthy();
  });
});
